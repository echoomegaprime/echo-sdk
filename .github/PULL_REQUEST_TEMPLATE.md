## What

## Why

## Testing

- [ ] `npm test` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run build` succeeds
- [ ] Added/updated tests for the change
- [ ] If touching `security.ts`/auth: added a test that would fail without the fix, and updated
      `SECURITY.md` if this closes a vulnerability class
